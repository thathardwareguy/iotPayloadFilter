{
  "name": "iotpayloadfilter",
  "version": "1.0.0",
  "description": "Lambda function to filter data from AWS IoT Core and store in Postgres",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./dist && tsc -p . && mkdir deploy && cp -r dist/* deploy/ && cp -r node_modules deploy/ && cd deploy && zip -r ../deploy.zip *",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build2": "rimraf ./dist && tsc -b"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thathardwareguy/iotPayloadFilter.git"
  },
  "author": "Kayode Alade",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/thathardwareguy/iotPayloadFilter/issues"
  },
  "homepage": "https://github.com/thathardwareguy/iotPayloadFilter#readme",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.70",
    "@types/node": "^16.7.13",
    "aws-sdk": "^2.113.0",
    "ts-loader": "^9.2.6",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1338.0",
    "graceful-fs": "^4.2.11",
    "pg": "^8.8.0",
    "pg-cloudflare": "^1.1.0",
    "pg-pool": "^3.5.2",
    "pgtools": "^0.3.2",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^3.0.2",
    "sequelize": "^6.26.0",
    "serverless": "^3.32.2",
    "serverless-esbuild": "^1.45.1",
    "serverless-plugin-typescript": "^2.1.4",
    "typeorm": "^0.3.11"
  }
}
